const defaultProps = {
  variant: 'solid',
  padding: 3,
  fontSize: 'md',
};

const solid = {
  backgroundColor: '#E9E9E9',
};

const Button = {
  defaultProps,
  variants: {
    solid,
  },
};

export default Button;

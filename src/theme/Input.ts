const defaultProps = {
  variant: 'solid',
  padding: 3,
  fontSize: 'md',
  color: '#FFFFFF',
  placeholderTextColor: '#AAAAAA',
};

const solid = {
  backgroundColor: '#3D3D3D',
};

const Input = {
  defaultProps,
  variants: {
    solid,
  },
};

export default Input;
